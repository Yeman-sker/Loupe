{
  "name": "@loupe/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.build-lib.json",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@loupe-server/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
